package com.example.todolist.view.addListItem

import androidx.appcompat.app.AppCompatActivity
import com.example.todolist.databinding.AddListItemActivityBinding

class AddListItemActivity : AppCompatActivity() {

    val binding by lazy {
        AddListItemActivityBinding.inflate(layoutInflater)
    }

}